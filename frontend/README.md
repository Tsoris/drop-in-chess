# Drop in Chess Frontend

React + TypeScript frontend for Drop in Chess.

## Requirements

- Node.js
- npm

## To Run

From the `frontend` directory:

```bash
npm install
npm run dev
```

## To Test
From the `frontend` directory:

```bash
npm test
```
